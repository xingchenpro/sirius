1.获取下一个兄弟节点值,html结构会把文本行当同胞元素处理
categoryList.push(obj[k].nextSibling.nodeValue);

2.JS获取checkBox
var obj = document.getElementsByName("categoryCheckbox");
   var categoryList = [];
   for(var k in obj){
      if(obj[k].checked)
         categoryList.push(obj[k].value);
       }
   alert(categoryList);

th:value="${category.categoryName}"